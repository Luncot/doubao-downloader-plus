# 手动验收清单（发版前）

> 机器能查的（版本一致、产物同步、功能特征）由 `pnpm smoke` 负责；
> 这一份只列**必须真实登录豆包/Dola 页面**才能验证的交互项。发版前逐项打勾，全程约 5 分钟。

**准备**：先跑 `pnpm smoke` 全绿；本地安装最新构建产物（Tampermonkey 装 `dist/doubao-downloader.user.js`，或浏览器"加载已解压的扩展程序"选 `dist/`；Plus 扩展用户解压 `doubao-downloader-plus.zip` 加载 `chrome-extension/`）。

## A. 豆包（doubao.com/chat）

- [ ] 进入含 AI 图片的对话，下载按钮出现在图片上/面板中
- [ ] 单张图片下载：文件能保存，**放大无水印标记**（或与页面预览对比无水印）
- [ ] 多图对话：批量勾选/全选 → ZIP 下载成功，压缩包可正常解压
- [ ] 15 秒视频（如有生成入口）：触发生成后能拿到无水印播放地址并下载
- [ ] 刷新页面（SPA 路由切换）后按钮仍可用，不重复注入

## B. Dola（dola.com/chat）

- [ ] 无水印视频下载主链路可用（console 可见 `[video] chrome-extension 已接管下载` / inject 日志）
- [ ] 下载的视频无水印（`lr=unwatermarked` 生效）
- [ ] 若走"我的创作"列表下载：能找到列表项并成功取址下载

## C. 通用

- [ ] 控制台无未捕获异常（重点关注 `JSON.parse` 拦截与其他脚本的兼容）
- [ ] 扩展 popup 能打开，显示当前版本 = 本版号
- [ ] 更新检测：本机与远程版本不同时按预期提示（新版不弹旧提示、旧版提示可更新）
- [ ] 批量下载中途不卡死、有进度反馈（或按版本行为）

## 发现问题的处理

在仓库记录复现步骤（页面、操作、console 日志片段），小步修复后：`pnpm build && pnpm smoke` 复验，
功能改动补测本清单对应项；确认无误再 `pnpm release` 出正式包。
